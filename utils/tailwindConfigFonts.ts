const tailwindConfig = require('../tailwind.config.js');

const tailwindCustomFonts = {
    sans: tailwindConfig.theme.fontFamily.sans,
    serif: tailwindConfig.theme.fontFamily.serif,
    northwell: tailwindConfig.theme.fontFamily.northwell,
};


export default tailwindCustomFonts;